import os
def rebase(session):
    # recreate/ rebase/ reset the database or json file here
    print(rebase)
    pass

def store_orders(current_orders, tick, session):
    # store orders in a temporary database / json file
    pass